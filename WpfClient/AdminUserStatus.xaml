<UserControl x:Class="WpfClient.AdminUserStatus"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfClient"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Border>
        <Grid>
            <Grid.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="#808080" Offset="0.3"/>
                    <GradientStop Color="#AEAEAE" Offset="0.8"/>
                    <GradientStop Color="#AEAEAE" Offset="0"/>
                </LinearGradientBrush>
            </Grid.Background>
            <Menu VerticalAlignment="Center" HorizontalAlignment="Center">
                <MenuItem x:Name="userItem" Header="User" VerticalAlignment="Stretch">
                    <MenuItem Header="Delete" Background="Gray" Click="MenuItem_Click"/>
                </MenuItem>
            </Menu>
        </Grid>
    </Border>
    
</UserControl>
